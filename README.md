# Storage-Service

DATABASE_URL=postgres://admin:admin@localhost:5432/storage_service?sslmode=disable

сервис для безопасного обмена данными между двумя пользвателями сайта

Для шифрования используются следующие алгоритмы:

-
-
-

каждый пользователь может создать себе ключи(приватные и публичные)  с ограниченым сроком годности и доверяет сайту их хранение.

данные передаются через форму.
Можно вбить их напрямую в поле ввода или выбрать файл.

сервис исключает промежуточное хранение данных в не зашифрованном виде.

позволяет хранить зашифрованные данные любого объема в облаке где угодно.

Алгоритмы шифрования пользователь может настроить для себя
